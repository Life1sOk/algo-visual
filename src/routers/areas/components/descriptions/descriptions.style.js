import styled from 'styled-components';

export const DescriptionContainer = styled.article`
    ${'' /* max-width: 500px; */}
    color: white;
    line-height: 27px;

    p {
        margin-bottom: 7px;
        font-size: 19px;
        padding: 5px 10px;
    }
`; 